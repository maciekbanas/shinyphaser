# Create assets folder in your project

Creates the default `inst/examples/assets/` directory in your package
project if it doesn't exist. Useful for storing your game's graphic
assets.

## Usage

``` r
phaser_create_assets()
```
